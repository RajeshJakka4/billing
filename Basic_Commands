created a repo in github
1. git clone githuburl
Red  ---------------->Green ------------------> None(git status)
Working Area -------> Stagging Area ---------> Local Repo ---------> remote Repo
             git add .             git commit -m "cm"       git push    
             git add fn
            ------------------------------------->
            git commit -am "cm"
            This Command only works with Modified files or already exsisting files and will not work with new files
Remote Repo ----------> Local Repo
          if in case you are not having that repo exsisting in your Local 
          1. git clone
          if you are already having that repo 
          1. git pull
git configutaion of user name and email 
git config --global user.name "RajeshJakka4"
git config --global user.email "rajeshjakka4@gmail.com"

Restore 
if you remove the files that have created accidentaly or by mistaken, we will be using git restore command.
1.git restore fn
